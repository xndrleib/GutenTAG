contract_id: synthgen.contract.lag_synchronization.v1
anomaly_type: lag-synchronization
intended_constraints:
  - dependence.lag_synchronization
canonical_witnesses:
  - lag_correlation_delta
  - correlation_delta
  - covariance_delta
allowed_artifacts:
  boundary.discontinuity: false
forbidden_shortcuts:
  - boundary.discontinuity
support_policy:
  type: localized_interval
  expected_primary_evidence: interior
  boundary_primary_allowed: false
channel_role_policy:
  intervention_min: 1
  context_min: 1
  canonical_min_projection_size: 2
realized_effect_schema:
  - requested_lag_steps
  - realized_lag_shift
  - lag_profile_shift
admission_policy:
  require_canonical_witness: true
  allow_shortcut_but_downgrade_claim: true
  require_realized_effect: true
